PORT=5000
DATABASE_URL=
NEXT_PUBLIC_AI_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:5000

JWT_SECRET=your-secret-here
FRONTEND_URL=http://localhost:3000

# Email
FROM_EMAIL=noreply@yourdomain.com
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# AWS SES (prod only)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Stripe
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# AI assistant (potential-ai) — shared service-JWT secret. Must equal the
# SERVICE_JWT_SECRET configured on the potential-ai deployment. Used to mint
# short-lived per-user tokens via GET /api/auth/copilot-token.
COPILOT_SERVICE_JWT_SECRET=
